<div class="flow-selection">
    {{^allFiles}}There are no flow files. To visualize your flow files put them in <code>{{flowDirectory}}</code>{{/allFiles}}
    {{#allFiles.length}}
        <div style="display: flex; flex-direction: row;">
            <div style="flex: 1 1 auto">
                Your flow files:
                <ul>
                    {{#allFiles}}
                        <li><a href="?files[]={{.}}">{{.}}</a></li>
                    {{/allFiles}}
                </ul>
            </div>

            <div style="flex: 1 1 auto">
                Compare with:
                <ul>
                    <li><a href="?file={{file}}">none</a></li>
                    {{#allFiles}}
                        <li><a href="?files[]={{file}}&files[]={{.}}">{{.}}</a></li>
                    {{/allFiles}}
                </ul>
            </div>
        </div>
    {{/allFiles.length}}
</div>
